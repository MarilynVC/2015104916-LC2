@model IEnumerable<_2015104916.Entities.Entities.Asiento>

@{
    ViewBag.Title = "Asientos - Índice";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Índice</h2>

<p>
    @Html.ActionLink("Añadir", "Create")
</p>
<table class="table">
    <tr>
        
        <th>
            N° de Serie
        </th>
        <th>
           Placa del Carro
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        
        <td>
            @Html.DisplayFor(modelItem => item.NumSerie)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Carro.Placa)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.AsientoId }) |
            @Html.ActionLink("Detalles", "Details", new { id=item.AsientoId }) |
            @Html.ActionLink("Eliminar", "Delete", new { id=item.AsientoId })
        </td>
    </tr>
}

</table>
